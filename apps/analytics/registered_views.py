from .views_registry import views_registry
from apps.blog.models import Post

# Registered views
# views_registry.register(namespace="blog", url_name="post", app_name="blog", model="post")
#
# views_registry.register(namespace="blog", url_name="post", app_name="blog", model=Post)
